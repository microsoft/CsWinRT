jobs: 
  - job:
    displayName: Build/Test Stage
    pool:
      type: windows
      
    timeoutInMinutes: 270
    # https://docs.microsoft.com/en-us/azure/devops/pipelines/process/phases?view=azure-devops&tabs=yaml#multi-job-configuration

    strategy:
      maxParallel: 10
      matrix:
        x64_Debug:
          BuildPlatform: 'x64'
          BuildConfiguration: 'debug'
        x86_Debug:
          BuildPlatform: 'x86'
          BuildConfiguration: 'debug'
        arm64_Debug:
          BuildPlatform: 'arm64'
          BuildConfiguration: 'debug'
        x64_Release:
          BuildPlatform: 'x64'
          BuildConfiguration: 'release'
        x86_Release:
          BuildPlatform: 'x86'
          BuildConfiguration: 'release'
        arm64_Release:
          BuildPlatform: 'arm64'
          BuildConfiguration: 'release' 
          
    variables:
      ob_outputDirectory: '$(Build.SourcesDirectory)\out'
      ob_artifactSuffix: $(BuildConfiguration)_$(BuildPlatform)
      StagingFolder: $(ob_outputDirectory)
      ob_sdl_prefast_enabled: true
      ob_sdl_prefast_runDuring: 'Build'
      ob_sdl_checkCompliantCompilerWarnings: true
      ob_symbolsPublishing_enabled: true
      ob_symbolsPublishing_indexSources: true
      ob_symbolsPublishing_searchPattern: '**/*.@(pdb|dll)'

    steps:
    - checkout: TestWinRT
      path: 's\src\TestWinRT'
      displayName: "Checkout TestWinRT"
   
# Build Steps 
    - template: CsWinRT-Build-Steps.yml@self
      parameters:
        BuildConfiguration: $(BuildConfiguration)
        BuildPlatform: $(BuildPlatform) 

#    - template: CsWinRT-Test-Steps.yml@self
#      parameters:
#        BuildConfiguration: $(BuildConfiguration)
#        BuildPlatform: $(BuildPlatform) 

# Signing
    - task: onebranch.pipeline.signing@1
      displayName: '🔒 Onebranch Signing for cswinrt binaries'
      condition: and(succeeded(), eq(variables['BuildConfiguration'], 'release'))
      inputs:
        command: sign
        signing_profile: external_distribution
        files_to_sign: |
          net10.0/WinRT.SourceGenerator.dll;
          net10.0/WinRT.Host.Shim.dll;
          net10.0/WinRT.Runtime.dll;
          native/WinRT.Host.dll;
          netstandard2.0/WinRT.Generator.Tasks.dll;
          net10.0/native/cswinrtinteropgen.exe;
          net10.0/native/cswinrtimplgen.exe;
          net10.0/native/cswinrtprojectiongen.exe;
          net10.0/native/cswinrtprojectionrefgen.exe;
          net10.0/native/cswinrtwinmdgen.exe;
        search_root: $(StagingFolder)

    - task: onebranch.pipeline.signing@1
      displayName: '🔒 Onebranch Signing for cswinrt binaries'
      condition: and(succeeded(), eq(variables['BuildConfiguration'], 'release'))
      inputs:
        command: sign
        cp_code: '400'
        files_to_sign: |
          native/WindowsRuntime.Internal.winmd;
          native/WinRT.Host.dll.mui;
        search_root: $(StagingFolder)

    - task: onebranch.pipeline.signing@1
      displayName: '🔒 Onebranch Signing for SDK projection binaries'
      condition: and(succeeded(), eq(variables['BuildConfiguration'], 'release'), eq(variables['_GenerateSdkProjection'], 'true'))
      inputs:
        command: sign
        signing_profile: external_distribution
        files_to_sign: |
          impl/*/WinRT.Sdk.Projection.dll;
          impl/*/WinRT.Sdk.Xaml.Projection.dll;
        search_root: $(StagingFolder)

  - job: Benchmarks
    displayName: Run Benchmarks
    condition: or(eq(variables['_RunBenchmarks'],'true'), eq(variables['Build.Reason'],'Schedule'))
    dependsOn: []
    pool:
      type: windows
      isCustom: true
      name: 'Azure Pipelines'
      vmImage: 'windows-2022'
    timeoutInMinutes: 120    
    variables:
      ob_outputDirectory: '$(Build.SourcesDirectory)\out'
      StagingFolder: $(ob_outputDirectory)
    steps:
    - template: CsWinRT-Benchmarks-Steps.yml@self

# Publish Benchmark Results
    - task: PublishPipelineArtifact@1
      displayName: Publish Benchmarks Results
      condition: always()
      inputs:
        targetPath: $(Build.SourcesDirectory)\src\BenchmarkDotNet.Artifacts\results\
        artifact: drop_BuildAndTest_Benchmarks

  # Seperate job to run certain tests
  # that can't run in the other environment.
  - job: Tests
    displayName: Run Tests
    condition: succeeded()
    dependsOn: []
    pool:
      type: windows
      isCustom: true
      name: 'Azure Pipelines'
      vmImage: 'windows-2022'
    timeoutInMinutes: 120
    strategy:
      matrix:
        debug:
          BuildConfiguration: 'debug'
        release:
          BuildConfiguration: 'release'
    variables:
      ob_outputDirectory: '$(Build.SourcesDirectory)\out'
      StagingFolder: $(ob_outputDirectory)
      BuildPlatform: 'x64' 
    steps:
    - checkout: self
      clean: true
      persistCredentials: true
      path: 's'
    - checkout: TestWinRT
      path: 's\src\TestWinRT'
      displayName: "Checkout TestWinRT"
      
# Build Steps 
    # Build the CsWinRT build tooling (the generator executables and
    # WinRT.Generator.Tasks.dll), not the whole solution. The standalone
    # "Build Object Lifetime Tests" step below needs this tooling, which is
    # produced via the solution's BuildDependency ordering rather than as a
    # ProjectReference a single-project build would build in time (otherwise
    # MSB4062). Targeting the projects by name builds just their closure
    # (plus WinRT.Generator.Core / WinRT.Projection.Writer), and the solution
    # maps the x64 platform to AnyCPU for these projects so the outputs land
    # where Directory.Build.props expects them.
    - template: CsWinRT-Build-Steps.yml@self
      parameters:
        BuildConfiguration: $(BuildConfiguration)
        BuildPlatform: $(BuildPlatform) 
        SetupForBuildOnly: true

    - task: MSBuild@1
      displayName: Build CsWinRT build tooling
      condition: succeeded()
      inputs:
        solution: $(Build.SourcesDirectory)\src\cswinrt.slnx
        msbuildArguments: /restore /t:WinRT_Generator_Tasks;WinRT_Impl_Generator;WinRT_Interop_Generator;WinRT_Projection_Generator;WinRT_Projection_Ref_Generator;WinRT_WinMD_Generator /p:CIBuildReason=CI,VersionNumber=$(VersionNumber),VersionString=$(Build.BuildNumber),AssemblyVersionNumber=$(WinRT.Runtime.AssemblyVersion),PublishBuildTool=true,BuildToolArch=$(CsWinRTBuildToolsArch)
        platform: $(BuildPlatform)
        configuration: $(BuildConfiguration)

    - task: MSBuild@1
      displayName: Build Object Lifetime Tests
      condition: succeeded()
      inputs:
        solution: $(Build.SourcesDirectory)\src\Tests\ObjectLifetimeTests\ObjectLifetimeTests.Lifted.csproj
        msbuildArguments: /restore /p:CIBuildReason=CI,solutiondir=$(Build.SourcesDirectory)\src\,VersionNumber=$(VersionNumber),VersionString=$(Build.BuildNumber),AssemblyVersionNumber=$(WinRT.Runtime.AssemblyVersion),GenerateTestProjection=true,AllowedReferenceRelatedFileExtensions=".xml;.pri;.dll.config;.exe.config"
        platform: $(BuildPlatform)
        configuration: $(BuildConfiguration)

# Run Object Lifetime Tests
    - task: VSTest@3
      displayName: Run Object Lifetime Tests
      condition: succeeded()
      # Some tests currently fail on CsWinRT 3.0; don't fail the pipeline on test failures for now.
      continueOnError: true
      inputs:
        testAssemblyVer2: Tests\ObjectLifetimeTests\bin\$(BuildPlatform)\$(BuildConfiguration)\net10.0-windows10.0.26100.1\win-$(BuildPlatform)\ObjectLifetimeTests.Lifted.build.appxrecipe
        searchFolder: $(Build.SourcesDirectory)\src

# Run Source Generator Tests
    - task: DotNetCoreCLI@2
      displayName: Run Source Generator Tests
      condition: succeeded()
      inputs:
        command: test
        projects: 'src/Tests/SourceGeneratorTest/SourceGeneratorTest.csproj'
        arguments: --diag $(StagingFolder)\unittest\test.log --logger trx;LogFilePath=UNITTEST-$(Build.BuildNumber).trx /nologo /m /p:configuration=$(BuildConfiguration);CIBuildReason=CI;solutiondir=$(Build.SourcesDirectory)\src\;VersionNumber=$(VersionNumber);VersionString=$(Build.BuildNumber);AssemblyVersionNumber=$(WinRT.Runtime.AssemblyVersion) -- RunConfiguration.TreatNoTestsAsError=true
        testRunTitle: Unit Tests
