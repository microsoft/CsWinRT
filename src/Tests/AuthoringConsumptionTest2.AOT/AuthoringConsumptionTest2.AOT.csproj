<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0-windows10.0.26100.1</TargetFramework>
    <Platforms>x64</Platforms>
    <RuntimeIdentifiers>win-x64</RuntimeIdentifiers>

    <!-- Source generator emits the merged ABI.{AssemblyName}.ManagedExports.GetActivationFactory
         dispatcher walking each referenced component's ManagedExports. -->
    <CsWinRTMergeReferencedActivationFactories>true</CsWinRTMergeReferencedActivationFactories>

    <!-- AOT publish into a single self-contained native dll exporting DllGetActivationFactory. -->
    <PublishAot>true</PublishAot>
    <NativeLib>Shared</NativeLib>
    <SelfContained>true</SelfContained>
    <OutputType>Exe</OutputType>
    <CustomNativeMain>true</CustomNativeMain>
    <IsTrimmable>true</IsTrimmable>
  </PropertyGroup>

  <ItemGroup>
    <!--
      All PRs UndefineProperties=PublishAot: this csproj is the AOT publish root.
      We pass PublishAot=true as a global from the consumer vcxproj's CsWinRTLink target
      so NuGet Restore pulls in the ILC runtime package. Without these UndefineProperties
      the global propagates transitively to managed-only PRs (e.g. Projections/Windows
      -> WinRT.Generator.Tasks targeting netstandard2.0) and triggers NETSDK1207.
    -->
    <ProjectReference Include="..\AuthoringTest\AuthoringTest.csproj">
      <!--
        Suppress AuthoringTest's own Directory.Build.props PublishAot block so it builds
        as a plain CsWinRT component library, not an exe with the WinAppSDK runtime
        payload. This .AOT host does the AOT linking.
      -->
      <Properties>_AuthoringTestSkipAotPublishConfig=true</Properties>
      <UndefineProperties>PublishAot</UndefineProperties>
    </ProjectReference>
    <ProjectReference Include="..\AuthoringTest2\AuthoringTest2.csproj">
      <UndefineProperties>PublishAot</UndefineProperties>
    </ProjectReference>
    <ProjectReference Include="..\..\Projections\Windows\Windows.csproj">
      <UndefineProperties>PublishAot</UndefineProperties>
    </ProjectReference>
    <ProjectReference Include="..\..\Authoring\WinRT.SourceGenerator2\WinRT.SourceGenerator2.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false">
      <UndefineProperties>PublishAot</UndefineProperties>
    </ProjectReference>
    <ProjectReference Include="..\..\cswinrt\cswinrt.vcxproj">
      <UndefineProperties>PublishAot</UndefineProperties>
    </ProjectReference>
  </ItemGroup>

</Project>
